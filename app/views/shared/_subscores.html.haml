%table.scores
  %thead
    %tr
      %th 
      %th Score
      %th Pts
      %th Max
  - scores.andand.each do |h|
    %tr
      %td= h[:name]
      %td= (h[:value].is_a?(Float) and h[:value] <= 1.0) ? "#{(h[:value] * 100).to_i}%" : h[:value]
      %td= h[:points]
      %td
        .relative
          = h[:possible]
          = render 'shared/tip', :tip => @tips["#{section}_#{h[:key]}".to_sym]

      