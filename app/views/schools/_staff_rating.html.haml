.staff-rating
  .axis
    .right 4
    .left 3
    .middle 3.5
  .bar
    .light-green
    .bronze
    .silver
    .gold
  - state_pct, score_pct = 100 * (state-3).round(2), 100 * (score.to_f-3).round(2)
  .pointer.state{:style => "right: #{100-state_pct}%;"} Program Average = #{state}
  - if school.teacher_survey_rating.downcase.include? 'not rated'
    %p.not-rated This program did not receive enough responses.
  - else
    .pointer.score{:style => "right: #{100-score_pct}%;"} This Program = #{score.to_f.round(2)}
