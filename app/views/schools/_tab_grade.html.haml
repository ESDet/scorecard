- if false
  %h2 #{grade[:total].blank? ? '?' : grade[:total]} / #{grade[:possible].blank? ? '?' : grade[:possible]}
- elsif false
  %br/
  %br/
  %h1= grade[:letter].to_s.length < 3 ? grade[:letter] : '?'  
- else
  = image_tag School.grade_image(grade[:letter].to_s), :class => 'el-icon'

