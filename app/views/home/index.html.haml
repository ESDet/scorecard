%h1 2014 Scorecard

%p.lead.centered Excellent Schools Detroit reviews Detroit schools each year and produces a scorecard that measures school quality.


#block-boxes-school-score-widget.block.block--boxes.block--boxes-simple.block--boxes-school-score-widget.by-zip
  %h2.block__title  Search for Schools by Location
  .block__content
    #boxes-box-school_score_widget.boxes-box
      .boxes-box-content
        .score-right
          #front-filter
            = render 'shared/filters', :long_names => true
        .clear_floats



#block-boxes-school-score-widget.block.block--boxes.block--boxes-simple.block--boxes-school-score-widget.by-name
  %h2.block__title Search for Schools by Name
  .block__content
    #boxes-box-school_score_widget.boxes-box
      .boxes-box-content
        .score-right
          #front-filter
            = form_tag '/search', { :method => :post, :class => 'form-byname search' } do
              = text_field_tag :q, nil, :placeholder => 'Search by school name', :autocomplete => 'off'
              .section-learn
                .l-content-right
                  %ul.menu
                    %li
                      %a#name-search{:href => '#'} Search
            :javascript
              $(document).ready(function() {
                var school_names = #{School.select('id, name').collect { |s| s.name }.to_json};
                $('input#q').typeahead({ source: school_names });
                $('#name-search').click(function(e) {
                  $('.form-byname').submit();
                  return false;
                });

                $('.btn-group.grades a').click(function(e) {
                  var kind = $(this).data('filter');
                  if(kind == 'ec') {
                    $('#top_k12').hide();
                    $('#top_ec').show();
                  } else {
                    $('#top_k12').show();
                    $('#top_ec').hide();
                  }
                  return false;
                });

              });
        .clear_floats



  

.clear_floats
%br/
%br/

